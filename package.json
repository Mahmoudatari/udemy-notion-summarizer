{
  "name": "udemy-transcript-extension",
  "version": "1.0.0",
  "description": "Chrome extension for processing Udemy transcripts with AI and Notion integration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "webpack --mode=production && cp popup.html dist/ && cp -r icons dist/",
    "dev": "webpack --mode=development --watch",
    "clean": "rm -rf dist",
    "build-extension": "npm run clean && npm run build"
  },
  "dependencies": {
    "@tryfabric/martian": "^1.2.4"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "babel-loader": "^9.1.3",
    "path-browserify": "^1.0.1",
    "url": "^0.11.4",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
